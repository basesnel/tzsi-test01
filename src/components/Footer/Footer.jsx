import styles from "./styles.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h2 className={styles.hidden}>footer with tapbar</h2>
    </footer>
  );
};

export default Footer;
