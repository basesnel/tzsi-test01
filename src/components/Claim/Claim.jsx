import styles from "./styles.module.css";

const Tips = () => {
  return (
    <section className={styles.section}>
      <h2 className={styles.hidden}>claim my cash</h2>
      <button className={styles.button}>claim</button>
    </section>
  );
};

export default Tips;
